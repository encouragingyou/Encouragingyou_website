# Prompt 16 To 17 Handoff

Prompt 16 locks the homepage architecture, but it does not do the full visual rebuild.

## What is now in place

- canonical homepage decision data in `site/src/content/homePage/default.json`
- explicit launch updates feed contract in `site/src/content/updatesFeed/default.json`
- homepage runtime now exposes:
  - action map
  - conversion stack
  - ordered section sequence
  - state surface
  - launch boundaries
- the current homepage route already benefits from:
  - four-programme coverage
  - corrected trust-card routing
  - broader community block routing into the community-support programme instead of back to sessions
- structured and editorial validators now understand the homepage contract
- unit coverage exists in `site/tests/home-page.test.mjs`

## Prompt 17 focus

- switch the production homepage to render from `home.sections` rather than the current compatibility fields
- build the missing section-level UI for:
  - trust strip
  - about teaser
  - updates teaser / update cards
  - contact teaser/panel
- treat `conversionStack` and `stateSurface` as binding, not advisory
- preserve the section order from Prompt 16 unless real implementation evidence forces a change

## Watch-outs

- do not pull quick actions back into global navigation config
- do not route broader community support back to `/sessions/`
- do not flatten trust items into one blanket safeguard link again
- do not fake update cards or duplicate recurring sessions inside the updates surface
- keep supporter routes subordinate to support-seeker clarity
