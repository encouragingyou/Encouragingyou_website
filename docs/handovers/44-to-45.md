# Prompt 44 To 45

Prompt 44 established the resilience contract and shipped the first shared degraded-state surfaces.

What is now canonical:

- `site/src/content/resilienceStates/default.json`
- `site/src/lib/content/resilience-state.js`
- `site/src/components/ui/ResiliencePanel.astro`
- `site/src/components/ui/MediaFallback.astro`
- route integrations for `/404/`, `/events-updates/`, `/sessions/`, `/contact/`, and the shared enquiry form

Prompt 45 should build automated UI and accessibility quality gates directly against these contracts.

Priority validation targets:

1. `404` response + recovery links
2. enquiry validation state
3. enquiry submission failure fallback
4. no-JS enquiry behavior
5. events filter empty fixture
6. sessions calendar-unavailable fixture
7. media fallback fixture

Do not replace the resilience content model with test-only strings. Use fixture setup or runtime interception so validation stays aligned with the shared contract. 
