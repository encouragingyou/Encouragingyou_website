# Prompt 47 To 48 Handoff

Prompt 47 hardened the runtime in `site/` with:

- shared security headers and hash-derived CSP in middleware
- Astro origin checks intentionally left off in `astro.config.mjs`
- same-origin, content-type, and body-size guards on the public POST APIs
- stricter client fetch modes for enquiry and analytics writes
- removal of inline style attributes that would have forced a weaker CSP
- `security:validate` for built-preview header/CSP/API checks
- `audit:policy` for dependency/advisory policy enforcement

Prompt 48 should build deployment/release infrastructure on top of this exact posture, not reinterpret it.

Carry forward:

- the middleware header contract in `site/src/lib/security/policy.js`
- the API request guards in `site/src/lib/security/request-guards.js`
- the blocking `security:validate` step inside `npm run validate`
- the advisory CI job now using `npm run audit:policy`

Prompt 48 decisions still required:

- where final HTTPS termination happens
- whether headers remain middleware-owned or move to host/CDN config
- how HSTS is guaranteed on production traffic
- where CSP/advisory/runtime failure signals land operationally
- how preview/staging/prod secrets are injected and rotated without entering source or client bundles
