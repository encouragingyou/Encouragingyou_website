# Prompt 42 To 43

Prompt 42 left the repo with a real performance contract:

- public route media now resolves through `/images/`, `/icons/`, and `/fonts/` rather than Astro-generated duplicates or copied prototype assets
- the standalone build now ships `0` Astro-generated media files under `dist/client/_astro`
- shared asset baseline is currently `89,231` raw CSS bytes (`13,925` gzip) and `10,010` raw JS bytes (`3,689` gzip)
- `npm run perf:validate` is part of `npm run validate`
- representative route measurements are written to `site/src/data/generated/performance-budget-report.json`

Prompt 43 must treat `site/src/lib/performance/policies.js` as the current measurement and budget source of truth.

Rules for Prompt 43:

- do not add analytics or tracking scripts without updating the explicit third-party and JS budget contract
- preserve the current single shared client script unless there is a measurable reason to split
- preserve the current public-media delivery path instead of reintroducing `astro:assets` image output for route illustrations
- keep the launch browser HTML cache policy at `public, max-age=0` unless deployment evidence justifies a safer differentiated rule
- if Prompt 43 adds any measurement runtime, rerun `npm run perf:validate` and the dedicated `performance-delivery` Playwright contract before changing broader QA
