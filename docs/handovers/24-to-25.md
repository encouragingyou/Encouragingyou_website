# Prompt 24 to 25 Handover

Prompt 24 rebuilt `/sessions/` as the live, schedule-first entry route for the current Saturday offers.

## What is now stable

- production sessions hub at `site/src/pages/sessions/index.astro`
- route-level live aggregate state in `site/src/lib/domain/session-hub-state.js`
- reusable `SessionsLiveRail` and `SessionHubCard` surfaces
- canonical sessions-hub copy in `site/src/content/routePages/default.json`
- sessions-hub unit coverage in `site/tests/session-hub-state.test.mjs` and `site/tests/sessions-page.test.mjs`
- sessions-hub browser coverage in `site/tests/e2e/contracts/sessions-hub.spec.mjs`

## What Prompt 25 should do

- rebuild `/sessions/cv-support/` as the first premium session-detail route in the now-stable session family
- keep the CV Support page operationally specific rather than retelling the whole Career Support programme
- expand Playwright around the programme-to-session path, breadcrumb returns, calendar handoff, and detail-route structure

## Watchouts

- do not reintroduce client-side next-date logic on the session-detail route
- do not weaken the parent-programme bridge just because the session page gets richer
- keep venue detail enquiry-led unless repo evidence changes the disclosure policy
- prefer strengthening assertions over relaxing them when Playwright finds a real defect
