# Prompt 48 To 49 Handoff

Prompt 48 established the release system around `site/`:

- Render blueprint in `render.yaml`
- manual PR previews on Render with runtime-enforced preview safety
- immutable release packaging in `site/scripts/package-release-bundle.mjs`
- manual production promotion in `.github/workflows/site-production-promote.yml`
- repo-owned post-deploy verification in `site/scripts/verify-deployment.mjs`

Prompt 49 should treat this deployment system as fixed infrastructure.

Do not reopen:

- preview noindex behavior
- release headers or health endpoint structure
- the GitHub workflow split between quality, release candidate, and production promote
- the protected-environment requirement for production secrets

Prompt 49 still needs real launch content and operational values for:

- the production public origin configured as `SITE_URL`
- final contact inbox handling and any downstream operational workflow
- approved partner/supporter assets
- real public contact details and service-area specifics
- verified legal-role names, accessibility review date, and any live processor details that remain placeholder-governed

If Prompt 49 needs to change content or legal copy, it should do so through canonical content files and the existing release pipeline, not by bypassing or weakening the deployment model.
